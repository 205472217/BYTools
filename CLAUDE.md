# ================================
#  QML + C++ 项目 全自动规则
# ================================

# 【回答和思考规则（必须严格遵守）】
1. 不要假设我清楚自己想要什么。动机或目标不清晰时，停下来讨论。
2. 目标清晰但路径不是最短的，直接告诉我并建议更好的办法。
3. 遇到问题追根因，不打补丁。每个决策都要能回答“为什么”。
4. 输出说重点，砍掉一切不改变决策的信息。

# 【核心架构铁律（必须严格遵守）】
1. C++ 只做：数据模型、网络、存储、算法、业务、状态管理
2. QML 只做：UI、布局、动画、交互、样式展示
3. 所有数据必须从 C++ Model 提供给 QML
4. QML 禁止处理复杂逻辑、循环、计算、存储
5. C++ 禁止直接操作 UI、绘制、改样式
6. C++ 类必须注册到 QML（QML_ELEMENT / 单例）
7. Model 必须实现：rowCount()、data()、roleNames()

# 【项目基础信息】
- 技术栈：C++17 + Qt 6.5+ + QML + CMake
- 架构分层：C++ 业务逻辑层 | QML UI 展示层（严格分离）
- 禁止：Qt5、JavaScript 复杂逻辑、QML 写业务、C++ 写界面

# 【C++ 编码规范】
- 缩进：4 空格
- 类名：大驼峰 UserModel
- 成员变量：m_ 前缀 m_userName
- 必须使用 Q_OBJECT / Q_GADGET
- 禁止裸指针长期持有（用 QPointer / unique_ptr）
- 禁止子线程操作 UI
- 信号槽使用正确连接方式

# 【QML 编码规范】
- 缩进：4 空格
- 书写顺序：id → anchors → 属性 → 信号
- 禁止硬编码宽高、颜色、文本
- 禁止在 QML 中写复杂逻辑
- 自定义组件首字母大写
- 使用#表示颜色的16进制数用大写字母
- 如已有可用的自定义组件qml，则使用自定义组件，不使用原生组件

# 【绝对禁止行为】
1. 禁止 QML 写业务逻辑
2. 禁止 C++ 写界面样式
3. 禁止混用 Qt5 / Qt6 API
4. 禁止不写 roleNames()
5. 禁止子线程操作 UI
6. 禁止删除已有接口
7. 禁止 console.log 作为业务逻辑

# 【工作模式】
你现在处于 QML + C++ 专家模式：
- 自动遵守分层
- 自动遵守规范
- 自动避免错误
- 全程记忆规则，不需要重复提醒
